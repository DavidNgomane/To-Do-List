/*import * as React from 'react';
import { Button, StyleSheet, Text, View } from 'react-native';

const Home = ({navigation}) => {
    return(
        <View>
            <Text>
                Hello Home Page
            </Text>
            <Button title="Go to todoList" onPress={() =>
                navigation.navigate("todoList")}>

            </Button>
        </View>
    );
};

export default Home; */